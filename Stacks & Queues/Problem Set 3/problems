
Q1. Integers in an array are unique and increasingly sorted. Please write a function/method to find an
integer from the array who equals to its index. For example, in the array {-3, -1, 1, 3, 5}, the number 
3 equals its index 3.

Q2. Implement a Dequeue using a linked-list, then insert and remove elements at both of its 
ends.

Q3. Implement a Queue using two Stacks, then test it for FIFO functionality.

Q4. Propose a solution to the O(N) complexity issue with priority queues. Analyse
the proposed solution, then implement and test it. 
